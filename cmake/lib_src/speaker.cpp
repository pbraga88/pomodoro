#include "speaker.h"
#include "common.h"

namespace SPEAKER {
    speaker::speaker() {
    }
    speaker::~speaker() {
    }
    
    void speaker::initialize() {
        debug("SPEAKER", "Initialized\n");
        /* INITIALIZATION ROUTINES */
        //[...]
    }

}