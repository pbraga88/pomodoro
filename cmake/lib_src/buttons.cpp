#include "buttons.h"
#include "common.h"

namespace BUTTONS {
    buttons::buttons () {
    }
    buttons::~buttons () {
    }

    void buttons::initialize() {
        debug("BUTTONS", "Initialized\n");
        /* INITIALIZATION ROUTINES */
        //[...]        
    }
}